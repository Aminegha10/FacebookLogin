const express = require("express");
const mongoose = require("mongoose");
const UserMovie = require("../Models/userMovie_model");

const markAsWatched = async (req, res) => {
  try {
  } catch (error) {}
};
module.exports = { markAsWatched };
